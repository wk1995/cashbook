<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.wk.cashbook">
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.WRITE_EXTERNA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <application
        android:name=".module.WkCashbookApp"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/module_icon"
        android:label="@string/module_name"
        android:theme="@style/Theme.AppCompat"
        tools:replace="android:label">
        <activity android:name=".account.wallet.WalletTradeRecodeActivity" />
        <activity android:name=".account.UpdateAccountOrWalletActivity" />
        <activity android:name=".account.info.AccountInfoActivity" />
        <activity android:name=".account.list.AccountListActivity" />
        <activity android:name="com.wk.cashbook.main.recode.info.TradeRecordInfoActivity" />
        <activity android:name=".main.CashBookMainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>